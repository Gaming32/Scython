async class MyClass(a, b, c) {
    def __init__(self, obj) {
        # self.value = obj;
    }

    def __add__(self, other) {
        # return MyClass(self.value + other.value);
    }

    def __repr__(self) {
        # return "MyClass(%r)" % self.value;
    }
}

if (__name__ == "__main__") {
    inst = MyClass();
}
